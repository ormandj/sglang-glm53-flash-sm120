# v0.3.1 release receipts

Hardware results apply to the internal image at `sha256:5c2c6fb8f5616d3b451d45d944f56248f0e81c6cc403b80aa8c296f8391b5e2d`, promoted from `v0.3.1-rc.1` to `v0.3.1` without rebuilding. The independently built public image is `sha256:6c5b7e6701fb64e1890f067cbad5bff2d5c979fb3952f520862a7dfb858cdea3`. The two registries have identical pinned image inputs and separate build provenance. Promotion receipts record each candidate/stable digest equality.

The qualification verdict records the exact image, build source and test scope. `exact-image.log` retains the isolated GPU/CPU checks; `upstream/` retains the route-packing microbenchmark and integer/sanitizer results. `startup/` and the final image/cold-C4 verdict cover memory acceptance. Hardware: two RTX PRO 6000 Blackwell Max-Q 96 GB GPUs, 300 W, TP2/EP1 over PCIe, 450,560-token context, four concurrent requests, 4,096-token prefill chunks and 32 GB HiCache per rank.

`qualification/quality/gsm8k-full/` retains the pinned grader's raw outputs and request profiles for all 1,319 questions. `gsm8k-quality-accounting.json` records both graders, their disagreements and the two budget-limited responses; both remain in the scores. The 73k/400k quality controls, continuation comparison and synthetic positional/host marker probes are retained under `qualification/`. Five host-load tool-decision checks passed; their verdict and source hash are published, while the private reconstructed-request fixture and full responses remain in the primary qualification repository.

All 24 engine cell analyzers passed. The original summary failed on a raw adaptive acceptance gauge of 1.3583. `qualification/engine-gate.status` and its log preserve that failure. `acceptance-gauge-attribution.json`, the source excerpt and `harness-correction-final.patch` explain the interval/current-width mismatch. `engine-gate-reviewed/` retains the unchanged cell inputs, corrected summary, hashes and analysis provenance. No measured cell was rerun or altered for this correction. The correction was approved by Codex and Claude, and 26 focused summarizer/comparator tests passed. All adaptive acceptance-rate gauge statistics are diagnostic ratios, not acceptance probabilities.

Strict-text 408k host recall failed because optional `MEMORY-CHECK:` labels changed after restoration. The unchanged ordered-marker oracle passed all four stages; both outcomes remain in the raw receipts. No byte-identical generation or single-run quality-equivalence claim is made. Engine fixed-window rates are not expected application throughput. See [BENCHMARKS.md](../../BENCHMARKS.md) for measurements and limits.

The complete profiling campaign, initial C1 decrease, follow-up, paired baseline data, reviews and private fixture receipts are retained in the primary `sglang-glm53-flash-sm120` qualification repository under `evidence/v0.3.1-release-20260906/`. This directory is a selected publication of those receipts, not a second hardware run.
