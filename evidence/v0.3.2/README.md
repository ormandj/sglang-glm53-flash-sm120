# v0.3.2 release receipts

The exact internal image `sha256:6b5ed4f7f8e6a56076f8446a11240dd1a4d9d49fdf62c07ad345026678890dc5` completed full release qualification and was promoted from `v0.3.2-rc.1` to `v0.3.2` without rebuilding. The independently built public image is `sha256:5f12516c84abb3a74f135ba43a18021b05c6ca14c186a6583087aefc245593fb`. Both were built from source `55ae7c65ee1e2cf02b4f3a85b00d06b5d8c839b0` with identical pinned image inputs. Each registry's promotion preserves its own candidate digest. Hardware measurements apply only to the internal digest.

Hardware and profile: two RTX PRO 6000 Blackwell Max-Q 96 GB GPUs at 300 W, TP2/EP1 over PCIe, W4A16 experts, FP8 KV, a 450,560-token pool, four running requests, 4,096-token prefill chunks and 32 GB HiCache per rank. `qualification-verdict.json` records the original release gates and supplemental completion; the exact-image CPU/GPU and installed FlashInfer receipts are under `gpu-validation/`.

`performance/` contains the mean/median output-after-MTP, forward and cold-prefill tables, including window metadata and source hashes. Decode output rates aggregate the active cohort and include reasoning. Fixed-output engine windows include a post-answer tail with path-dependent acceptance. These controlled measurements do not necessarily represent real-world performance. The C1 repetitive-ledger probe has six retained repetitions per context; its measured plateau output/forward ratio is separate from the server's adaptive acceptance gauge. All six varied-prompt probes reached their 4,096-token cap, so their rates are not completed-answer throughput. Original probe receipts are under `supplemental-throughput/`.

All 24 engine analyzers passed. `resolved-workload-comparison.json` checks every resolved AIPerf setting except the artifact output directory, plus random seeds, against retained v0.3.1. The saved config templates also match byte for byte. C1/r03's adaptive acceptance-rate gauge exceeded one and remains diagnostic telemetry; values below one can also mix draft-width windows and are not reliable probabilities. No analyzer repair or cell rerun was needed.

This is a filtered publication of the primary evidence. The original `engine-gate/SHA256SUMS` includes files deliberately omitted here, including server command lines and per-scrape exports; it is retained for provenance, not as a completeness claim for this subset. `publication-manifest.json` lists the copied files and their hashes. The complete in-pod engine tree remains in the qualified cache. The primary repository retains the filtered exports and their original source hashes.

Full GSM8K scored 1,279/1,319 with unchanged GLM-aware grading and 1,171/1,319 with pinned AIPerf, with no request errors. One capped response remains in both denominators. `gsm8k-quality-accounting.json` preserves all disagreements, including the correct answer whose later bold time label confuses the GLM-aware extractor. The 73k/400k controls scored 142/150 and 144/150. `paired-quality.json` retains matched lost/gained question IDs; one run per image establishes neither quality improvement nor equivalence.

Continuation rescoring agreed on 767/767 next-token choices, and cold/device recall preserved all seven ordered markers. Five confirmed host-load cycles preserved the expected tool decision; their verdict and source hash are public, while the private reconstructed request and full responses remain in the primary repository. The separate synthetic 408k host probe passed all four ordered-marker stages. Its strict-text check differed in optional `MEMORY-CHECK:` labels; both the original failure and the pre-existing marker-oracle pass are retained. Startup and final 7680x4320-image/independent-cold-C4 checks passed with zero restarts.

See [BENCHMARKS.md](../../BENCHMARKS.md) for measurements and known limits. The complete qualification record and reviews are in the primary project under `evidence/v0.3.2-rebuild-20260906/`.

The promoted stable Deployment also passed startup acceptance with zero restarts: its first sampled thinking chat completed in 0.410 seconds (0.285-second TTFT), followed by a 7680x4320 image and four independent cold 4k prompts. `gitops-stable.json` and `stable-startup/` retain the deployment identity and receipts. This boot reused the qualified candidate cache after its stable-name handoff; fresh-cache first-boot qualification is recorded separately.
